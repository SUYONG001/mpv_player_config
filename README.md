# 🎬 mpv 播放器部署与配置还原指南

> （uosc 与 ModernX 共存架构 + 全量中文注释版）
>
> **适用平台**：Windows 主推（兼顾 macOS / Linux 跨平台）
>
> **核心设计**：ModernX 全面负责主界面交互 + uosc 专精右键功能菜单

---

## 📖 目录

- [一、核心架构与特性](#一核心架构与特性)
  - [1.1 uosc 与 ModernX 分工](#11-uosc-与-modernx-分工)
  - [1.2 分辨率自适应 Profile](#12-分辨率自适应-profile)
  - [1.3 音频专项优化](#13-音频专项优化)
  - [1.4 解码方式：硬解与软解](#14-解码方式硬解与软解)
- [二、零基础部署步骤](#二零基础部署步骤)
- [三、功能验证清单](#三功能验证清单)
- [四、快捷键速查表](#四快捷键速查表)
- [五、脚本与配置速查](#五脚本与配置速查)
  - [5.1 脚本职能](#51-脚本职能)
  - [5.2 配置文件职能](#52-配置文件职能)
- [六、进度条主题色库](#六进度条主题色库)
- [七、跨平台与维护指南](#七跨平台与维护指南)

---

## 一、核心架构与特性

### 1.1 uosc 与 ModernX 分工

为解决两大主流 UI 框架界面重叠与功能冲突的问题，本方案实施了清晰的视觉与功能分工：

* **ModernX（`modernx.lua`）—— 主界面与交互渲染**
  * 全权负责底部进度条、悬浮控制栏、顶部标题栏的视觉呈现。
  * 深度联动 `thumbfast.lua` 抓取进度条预览缩略图。
  * 接管播放器空闲时的 Logo 展示与纯音频状态指示。

* **uosc 框架 —— 功能菜单与辅助指示引擎**
  * 全权负责鼠标右键与 <kbd>Tab</kbd> 唤出的多级半透明上下文菜单。
  * 提供播放列表导航面板、音轨/字幕直选面板与章节跳转列表。
  * **精准元素保留**：通过 `uosc.conf` 仅保留无边框细线边框（`window_border`）、右侧垂直音量滑块（`volume`）、流媒体缓冲旋转动画（`buffering_indicator`）及暂停闪烁图标（`pause_indicator`）。
  * **精准元素剔除**：强制禁用顶栏（`top_bar`）、进度条（`timeline`）、控制栏（`controls`）、空闲图标（`idle_indicator`）与音频指示（`audio_indicator`），彻底杜绝与 ModernX 发生视觉冲突。

<img width="962" height="563" alt="视频预览" src="https://github.com/user-attachments/assets/109adaf0-dd9f-4ca9-9934-fdaa97cda615" />


> **实机效果预览 · 视频播放协同**：左侧为鼠标右键唤出的 **uosc 半透明深色菜单**（支持倍速、轨道、滤镜快速直选），底部为 **ModernX 罗兰电竞紫进度条与控制栏**，鼠标悬停在时间轴上时，由 **Thumbfast** 秒级拉起高清原画缩略图，整体界面清爽无重叠。

### 1.2 分辨率自适应 Profile

全面采用原生视频分辨率进行针对性优化：

* **中等分辨率区间（`720P ≤ height < 2160`）**：自动激活 2 轮强效去色带（Deband），抚平动画雾面与暗部渐变断层。
* **超高清区间（`width ≥ 2560`）**：自动关闭去色带，完整保留 2K/4K 原盘高码率原生细节与纹理。
* **低清老旧视频（`height ≤ 720`）**：自动提升抗振铃（Anti-ringing）系数至 `0.8`，有效抑制压缩杂讯与边缘光晕。

### 1.3 音频专项优化

* **有封面音频**：自动居中展示内嵌封面或同目录封面图，按 `audio_scale_height` 缩放。
* **无封面音频**：`visualizer.lua` 自动挂载 `showcqtbar` 滤镜，渲染音阶标尺彩虹频谱。
* **音视频独立音量**：`volume_fix.lua` 按文件类型分别应用 `audio_vol` / `video_vol`。
* **拖动进度条防刺耳**：`quack.lua` 在拖动时自动降低音量，松手后平滑恢复。
* **窗口标题自定义**：`title_filename.lua` 读取 `title_filename.conf`，默认显示内嵌标题（无则回退文件名）。

### 1.4 解码方式：硬解与软解

mpv 支持两种视频解码方式，各有适用场景：

| 对比项 | 硬解（硬件解码） | 软解（软件解码） |
| :--- | :--- | :--- |
| **原理** | 利用显卡专用解码电路（NVDEC / D3D11VA 等） | 利用 CPU 通用计算解码 |
| **CPU 占用** | 极低 | 较高，取决于分辨率和编码 |
| **功耗** | 低，适合笔记本续航 | 高，高负载时风扇可能起飞 |
| **兼容性** | 依赖显卡支持，老旧或冷门编码可能不支持 | 几乎支持所有格式 |
| **画质** | 通常足够好，部分场景略逊于软解 | 理论最优，但差异通常难以察觉 |

**如何查看当前是硬解还是软解？**

* **方法一**：播放视频时按 <kbd>i</kbd> 调出统计面板，查看 `Video` 一栏。
  * 若显示 `[nvdec]`、`[d3d11va]` 或带有 `hardware` 字样 → **硬解**
  * 若只显示编码名称（如 `H.264 / AVC`）→ **软解**

* **方法二**：按 <kbd>~</kbd> 打开控制台，输入以下命令并回车：
  ```lua
  print(mp.get_property("hwdec-current"))
  ```
  * 返回 `no` → **软解**
  * 返回 `nvdec`、`d3d11va` 等 → **硬解**

**如何配置？**

在 `mpv.conf` 中：

```ini
# 自适应硬解（推荐）：优先硬解，不支持时自动回退软解
hwdec=auto-safe

# 强制软解
#hwdec=no

# 指定 NVIDIA 硬解（仅 N 卡）
#hwdec=nvdec
```

> 当前配置使用 `hwdec=auto-safe`，它会自动选择最稳妥的硬解接口（如 `d3d11va`），遇到不支持的格式则回退软解，兼顾兼容性与性能。

---

## 二、零基础部署步骤

### 1. 下载主程序并解压

* 从 [GitHub](https://github.com/mpv-player/mpv/releases/tag/git-release) 官方构建源获取最新的 64 位构建包。
* 将下载的压缩包解压至本地安装目录（推荐路径：`C:\mpv\`）。

### 2. 导入便携配置文件

* 将本仓库的全部配置文件与文件夹放入 mpv 根目录下的 `portable_config` 目录中。
* 确保 `portable_config` 包含 `mpv.conf`、`input.conf`、`fonts/`、`scripts/` 以及 `script-opts/`。

### 3. 校验并修正 `thumbfast.conf`（关键步骤）

由于缩略图依赖后台静默调用 mpv 进程，换电脑或修改安装路径后必须核实该项：

* 打开 `portable_config\script-opts\thumbfast.conf`。
* 确认 `mpv_path=` 指向当前设备上的实际绝对路径，或者配置为自动检测：

```ini
mpv_path=C:\mpv\mpv.exe
# 或直接填写：mpv_path=mpv
```

### 4. （可选）关联文件格式

* 以管理员身份运行 mpv 目录下的 `mpv-register.bat` 即可一键绑定系统常见媒体格式。
* 若不绑定，亦可直接将视频文件拖入 `mpv.exe` 窗口或双击视频选择用 `mpv` 打开。

---

## 三、功能验证清单

完成部署后，拖拽任意测试视频进入播放器，依次核对以下状态：

* [ ] **画质与渲染**：按 <kbd>i</kbd> 开启性能面板，观察 `Dropped Frames`（丢帧数）为 0 或维持极低水准。
* [ ] **显卡策略**：已在 NVIDIA 控制面板中为 `mpv.exe` 设置“电源管理模式 → 最高性能优先”，并将“监视器技术”设为“固定刷新率”（或在 Windows 图形设置中将其设为“高性能”）。
* [ ] **主界面弹出**：鼠标滑动至窗口底部，ModernX 罗兰紫进度条顺畅上浮弹出。
* [ ] **缩略图预览**：光标在进度条上方任意滑动，悬停位置的缩略图能够秒级渲染且无报错弹窗。
* [ ] **uosc 悬浮菜单**：点击鼠标右键或按 <kbd>Tab</kbd> 键，半透明菜单正常展示且文字为简体中文。
* [ ] **音量与辅助指示**：屏幕右侧垂直音量条调节正常，暂停时中央有闪烁暂停标，流媒体缓冲有旋转图标。
* [ ] **拖动防刺耳**：按住进度条拖动时音量自动降低，松手后平滑恢复，无刺耳爆音。
* [ ] **窗口标题**：标题栏显示内容与 `title_filename.conf` 中 `title_format` 配置一致（默认显示内嵌标题，无则回退文件名）。
* [ ] **解码方式**：按 <kbd>i</kbd> 查看 `Video` 栏，确认当前视频使用的是硬解还是软解（参考 [1.4 节](#14-解码方式硬解与软解)）。两种方式均可正常播放。
* [ ] **动态 Profile 触发**：
  * 播放 1080P 片源时，按 <kbd>Shift</kbd> + <kbd>i</kbd> 切至第 4 页可观察到 `Mid-Resolution-Deband` 预设处于激活状态。
  * 播放 4K 原盘片源时，去色带自动关闭。
* [ ] **纯音频播放专项**：
  * 播放 `.flac`、`.mp3`、`.wav` 等音频时，视频插值与升频后处理滤镜全自动注销卸载。
  * 音轨带有内嵌封面则居中展示封面；无封面时自动渲染彩虹色高动态 CQT 柱状频谱图。

<img width="482" height="293" alt="音频预览" src="https://github.com/user-attachments/assets/2d2e779c-2b1c-4c0f-8b5a-aaf3ee19b6b9" />

> **实机效果预览 · 音频频谱可视化**：播放无封面音频文件（如 `.wav`）时，`visualizer.lua` 自动挂载 `showcqtbar` 滤镜，以音阶（C/D/E/F/G/A/B）标尺动态呈现高精度彩虹频谱，同时底层自动切断无意义的画面插值与去色带计算，大幅节省 GPU 资源。

---

## 四、快捷键速查表

| 功能操作 | 默认按键绑定 | 归属模块 / 效果 |
| :--- | :--- | :--- |
| **暂停 / 继续** | <kbd>Space</kbd> / <kbd>鼠标左键</kbd> | 基础播控 |
| **全屏 / 窗口化** | <kbd>f</kbd> / <kbd>Enter</kbd> | 窗口状态切换 |
| **退出全屏** | <kbd>Esc</kbd> | 快捷退出全屏 |
| **音量调节** | <kbd>↑</kbd> / <kbd>↓</kbd> / <kbd>鼠标滚轮</kbd> | 步进 2% 调节（上限锁定 100%） |
| **静音切换** | <kbd>m</kbd> | 快速静音 |
| **微调进度** | <kbd>←</kbd> / <kbd>→</kbd> | 步进快退 / 快进 5 秒 |
| **大跨度跳转** | <kbd>Ctrl</kbd> + <kbd>←</kbd> / <kbd>→</kbd> | 步进快退 / 快进 60 秒 |
| **字幕轨道选择** | <kbd>j</kbd> | 呼出 uosc 字幕直选菜单 |
| **音频轨道选择** | <kbd>a</kbd> | 呼出 uosc 音轨直选菜单 |
| **视频轨道选择** | <kbd>v</kbd> | 呼出 uosc 视频轨直选菜单 |
| **去色带开关** | <kbd>b</kbd> | 手动开/关 Deband 滤镜 |
| **性能统计监控** | <kbd>i</kbd> | 显示 / 隐藏 OSD 渲染监控与丢帧统计 |
| **主功能菜单** | <kbd>鼠标右键</kbd> / <kbd>Tab</kbd> | 唤出 / 收起 uosc 核心交互面板 |
| **退出程序** | <kbd>q</kbd> | 安全退出进程并保存播放进度 |

---

## 五、脚本与配置速查

### 5.1 脚本职能

`portable_config/scripts/` 下所有 Lua 脚本的职责与配置文件对应关系：

| 脚本 | 对应配置 | 核心职责 |
| :--- | :--- | :--- |
| **`uosc/main.lua`** | `script-opts/uosc.conf` | uosc 框架入口，负责右键菜单、播放列表、轨道选择等交互 |
| **`modernx.lua`** | `script-opts/osc.conf` | ModernX 主界面渲染，负责底部进度条、控制栏、顶部标题 |
| **`thumbfast.lua`** | `script-opts/thumbfast.conf` | 后台子进程生成进度条悬停缩略图 |
| **`autoload.lua`** | `script-opts/autoload.conf` | 自动扫描同目录文件并加入播放列表 |
| **`playlistmanager.lua`** | `script-opts/playlistmanager.conf` | 播放列表管理器，支持排序、反转、随机、保存/加载 |
| **`visualizer.lua`** | `script-opts/visualizer.conf` | 纯音频播放时渲染 CQT 频谱可视化 |
| **`volume_fix.lua`** | `script-opts/volume_fix.conf` | 按文件类型应用独立音量与音频专属渲染设置 |
| **`quack.lua`** | `script-opts/quack.conf` | 拖动进度条时自动降低音量，避免音频重启爆音 |
| **`title_filename.lua`** | `script-opts/title_filename.conf` | 自定义窗口标题格式，支持 UTF-8 截断 |
| **`stats.lua`** | `script-opts/stats.conf` | 性能统计面板（通常为 mpv 内置，无需额外下载） |

### 5.2 配置文件职能

| 配置文件 | 相对路径 | 核心管理职责 |
| :--- | :--- | :--- |
| **`mpv.conf`** | `portable_config/` | 视频输出核心、D3D11 渲染驱动、升降频算法与分辨率 Profile 条件触发器 |
| **`input.conf`** | `portable_config/` | 键盘鼠标按键映射、uosc 菜单绑定与隐藏命令集 |
| **`osc.conf`** | `script-opts/` | ModernX 界面尺寸、滑动动画、按钮逻辑与十六进制 BGR 进度条色库 |
| **`uosc.conf`** | `script-opts/` | uosc 右键菜单外观、多语言配置、元素屏蔽黑名单（`disable_elements`） |
| **`thumbfast.conf`** | `script-opts/` | 缩略图生成上限尺寸、静默调用进程绑定及网络流媒体缩略图开关 |
| **`visualizer.conf`** | `script-opts/` | 音频可视化类型选择、渲染画质分级、高度占比与启用条件 |
| **`volume_fix.conf`** | `script-opts/` | 视频与音频独立音量初值设定、纯音频模式封面渲染上限尺寸 |
| **`quack.conf`** | `script-opts/` | 拖动进度条时的降音幅度（`duckratio`）与恢复时长（`ducksecs`） |
| **`title_filename.conf`** | `script-opts/` | 窗口标题格式（`title_format`）与 UTF-8 截断参数 |
| **`playlistmanager.conf`** | `script-opts/` | 播放列表管理器行为（排序、缩略名、自定义按键等） |
| **`autoload.conf`** | `script-opts/` | 目录递归深度、连播媒体格式白名单与文件隐藏过滤规则 |
| **`stats.conf`** | `script-opts/` | 性能统计面板悬浮停留时间及避让 uosc 顶栏的边距控制 |

---

## 六、进度条主题色库

ModernX 界面颜色遵循 **BGR（蓝-绿-红）** 排序，与传统 Web 端的 RGB 完全相反（例如网页 RGB 紫色 `#9933FF` 在配置中须转换为 `FF3399`）。

若需更换主题色，只需编辑 `portable_config/script-opts/osc.conf`，将末尾的 `seekbarfgcolor=` 改为下列代码即可：

| 分类系列 | 主题色彩名称 | BGR 配置代码 | 视觉效果描述 |
| :--- | :--- | :---: | :--- |
| **经典简约系** | 极简纯白 | `FFFFFF` | 纯净高亮，适合极简无干扰观影 |
| | 油管正红 | `0000FF` | 醒目鲜明，经典流媒体风格 |
| | 深海湛蓝 | `FF0000` | 沉稳冷峻，夜间使用不刺眼 |
| | 暖阳金橙 | `00B3FF` | 温和舒适，视觉辨识度高 |
| **动漫主题系** | 哔哩粉 | `9972FB` | 柔和粉红，二次元观影首选 |
| | 初音绿 | `BBC539` | 标志性葱绿配色，活力醒目 |
| | 初号机紫 | `A95178` | 沉稳微暗的经典机体配色 |
| | **罗兰电竞紫（当前默认）** | **`FF3399`** | **高饱和暗夜电竞紫，与 uosc 菜单浑然一体** |
| **高对比霓虹系** | 赛博青 | `FFFF00` | 亮眼电光青，暗色画面极易辨识 |
| | 霓虹粉 | `7A10FF` | 艳丽霓虹质感，前卫视觉风格 |
| | 警告黄 | `00FFFF` | 明黄撞色，进度定位清晰直接 |
| | 荧光绿 | `00FF00` | 高对比荧光绿，清晰醒目 |

---

## 七、跨平台与维护指南

1. **跨系统部署与平台微调**
   * **Windows 平台**：强制保留 `gpu-api=d3d11` 以获得最佳的资源开销与流畅度。
   * **macOS / Linux 平台**：在 `mpv.conf` 中将 `gpu-api=d3d11` 改为 `gpu-api=vulkan` 或直接注释该行。
   * **脚本通用性**：内置的 8 份核心 Lua 脚本均为标准跨平台实现，无需更动内部代码。

2. **驱动更新维护**
   * 每次显卡驱动重装或更新后，建议进入显卡控制面板重新确认 `mpv.exe` 是否维持“固定刷新”与“最高性能优先”策略。

3. **解码方式按需切换**
   * 当前默认使用 `hwdec=auto-safe`，兼顾硬解性能与软解兼容性。
   * 若需强制软解以获得绝对兼容，可将 `mpv.conf` 中的 `hwdec` 改为 `no`。
   * 若使用 NVIDIA 显卡且希望指定 NVDEC，可改为 `nvdec`（需确认显卡支持对应编码）。

4. **版本迭代备份建议**
   * 后续针对快捷键或滤镜进行优化后，建议将整个 `portable_config` 目录使用 7-Zip 重新打包存档，并同步更新文档日期与版本号。
